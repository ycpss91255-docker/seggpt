"""Layer 1 utility modules.

Slimmed-down ports of ``generative_services.utils.*``. Only the symbols
referenced by the SegGPT runtime kernel are kept; other helpers from
the upstream package (random naming, logger choosers, etc.) were
dropped because Layer 1 does not need them.
"""
